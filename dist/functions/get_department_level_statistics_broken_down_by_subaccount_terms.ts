 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/analytics/terms/{term_id}/statistics_by_subaccount";
  

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_department_level_statistics_broken_down_by_subaccount_terms = async (token: string, baseUrl: string, params: {
  "account_id": string;"term_id": string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as void
}
