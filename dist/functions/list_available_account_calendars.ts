 //Generated by CoasterFan5
  const baseUrl = "/v1/account_calendars";
  import type {accountcalendarsAccountCalendartotalresultsinteger} from "../models/accountcalendarsAccountCalendartotalresultsinteger"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_available_account_calendars = async (token: string, baseUrl: string, params: {
  "search_term"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as accountcalendarsAccountCalendartotalresultsinteger[]
}
