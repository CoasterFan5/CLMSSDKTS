 //Generated by CoasterFan5
  const baseUrl = "/v1/sections/{id}";
  import type {Section} from "../models/Section"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const edit_section = async (token: string, baseUrl: string, params: {
  "id": string;"course_section[name]"?: string;"course_section[sis_section_id]"?: string;"course_section[integration_id]"?: string;"course_section[start_at]"?: undefined;"course_section[end_at]"?: undefined;"course_section[restrict_enrollments_to_section_dates]"?: boolean;"override_sis_stickiness"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Section
}
