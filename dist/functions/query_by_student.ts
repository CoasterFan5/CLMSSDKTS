 //Generated by CoasterFan5
  const baseUrl = "/v1/audit/grade_change/students/{student_id}";
  import type {GradeChangeEvent} from "../models/GradeChangeEvent"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const query_by_student = async (token: string, baseUrl: string, params: {
  "student_id": string;"start_time"?: undefined;"end_time"?: undefined
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as GradeChangeEvent[]
}
