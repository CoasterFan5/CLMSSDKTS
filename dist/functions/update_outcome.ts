 //Generated by CoasterFan5
  const baseUrl = "/v1/outcomes/{id}";
  import type {Outcome} from "../models/Outcome"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_outcome = async (token: string, baseUrl: string, params: {
  "id": string;"title"?: string;"display_name"?: string;"description"?: string;"vendor_guid"?: string;"mastery_points"?: number;"ratings[description]"?: unknown;"ratings[points]"?: unknown;"calculation_method"?: string;"calculation_int"?: number;"add_defaults"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Outcome
}
