 //Generated by CoasterFan5
  const baseUrl = "/lti/courses/{course_id}/line_items/{id}";
  import type {LineItem} from "../models/LineItem"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_line_item = async (token: string, baseUrl: string, params: {
  "course_id": string;"id": string;"scoreMaximum"?: number;"label"?: string;"resourceId"?: string;"tag"?: string;"startDateTime"?: string;"endDateTime"?: string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as LineItem
}
