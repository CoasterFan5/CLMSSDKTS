 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/course_pacing/{id}";
  import type {CoursePace} from "../models/CoursePace"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_course_pace = async (token: string, baseUrl: string, params: {
  "id": string;"course_id": number;"course_pace_id": number
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as CoursePace
}
