 //Generated by CoasterFan5
  const baseUrl = "/lti/developer_key/update_public_jwk";
  import type {DeveloperKey} from "../models/DeveloperKey"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_public_jwk = async (token: string, baseUrl: string, params: {
  "public_jwk": undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as DeveloperKey
}
