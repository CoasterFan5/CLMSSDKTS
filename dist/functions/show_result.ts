 //Generated by CoasterFan5
  const baseUrl = "/lti/courses/{course_id}/line_items/{line_item_id}/results/{id}";
  import type {Result} from "../models/Result"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_result = async (token: string, baseUrl: string, params: {
  "course_id": string;"line_item_id": string;"id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Result
}
