 //Generated by CoasterFan5
  const baseUrl = "/v1/audit/course/accounts/{account_id}";
  import type {CourseEvent} from "../models/CourseEvent"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const query_by_account = async (token: string, baseUrl: string, params: {
  "account_id": string;"start_time"?: undefined;"end_time"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as CourseEvent[]
}
