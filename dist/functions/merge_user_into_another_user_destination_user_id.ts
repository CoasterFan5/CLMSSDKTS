 //Generated by CoasterFan5
  const baseUrl = "/v1/users/{id}/merge_into/{destination_user_id}";
  import type {User} from "../models/User"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const merge_user_into_another_user_destination_user_id = async (token: string, baseUrl: string, params: {
  "id": string;"destination_user_id": string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as User
}
