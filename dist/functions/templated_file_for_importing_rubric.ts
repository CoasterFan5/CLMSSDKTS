 //Generated by CoasterFan5
  const baseUrl = "/v1/rubrics/upload_template";
  import type {aCSVfileintheformatthatcanbeimported} from "../models/aCSVfileintheformatthatcanbeimported"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const templated_file_for_importing_rubric = async (token: string, baseUrl: string, params: {
  
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as aCSVfileintheformatthatcanbeimported
}
