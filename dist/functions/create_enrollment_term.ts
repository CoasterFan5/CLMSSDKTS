 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/terms";
  import type {EnrollmentTerm} from "../models/EnrollmentTerm"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_enrollment_term = async (token: string, baseUrl: string, params: {
  "account_id": string;"enrollment_term[name]"?: string;"enrollment_term[start_at]"?: undefined;"enrollment_term[end_at]"?: undefined;"enrollment_term[sis_term_id]"?: string;"enrollment_term[overrides][enrollment_type][start_at]"?: undefined;"enrollment_term[overrides][enrollment_type][end_at]"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as EnrollmentTerm
}
