 //Generated by CoasterFan5
  const baseUrl = "/v1/media_objects/{media_object_id}/media_tracks";
  import type {MediaTrack} from "../models/MediaTrack"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_media_tracks_for_media_object_or_attachment_media_objects = async (token: string, baseUrl: string, params: {
  "media_object_id": string;"include"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as MediaTrack[]
}
