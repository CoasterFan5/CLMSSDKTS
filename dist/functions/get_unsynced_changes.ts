 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/blueprint_templates/{template_id}/unsynced_changes";
  import type {ChangeRecord} from "../models/ChangeRecord"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_unsynced_changes = async (token: string, baseUrl: string, params: {
  "course_id": string;"template_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as ChangeRecord[]
}
