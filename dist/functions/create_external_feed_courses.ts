 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/external_feeds";
  import type {ExternalFeed} from "../models/ExternalFeed"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_external_feed_courses = async (token: string, baseUrl: string, params: {
  "course_id": string;"url": string;"header_match"?: boolean;"verbosity"?: string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as ExternalFeed
}
