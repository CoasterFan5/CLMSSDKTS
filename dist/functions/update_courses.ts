 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/courses";
  import type {Progress} from "../models/Progress"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_courses = async (token: string, baseUrl: string, params: {
  "account_id": string;"course_ids": unknown;"event": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Progress
}
