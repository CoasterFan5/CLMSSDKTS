 //Generated by CoasterFan5
  const baseUrl = "/v1/shared_brand_configs/{id}";
  import type {SharedBrandConfig} from "../models/SharedBrandConfig"

  const method = "DELETE"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const un_share_brandconfig_theme = async (token: string, baseUrl: string, params: {
  "id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as SharedBrandConfig
}
