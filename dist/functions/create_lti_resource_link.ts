 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/lti_resource_links";
  import type {LtiResourceLink} from "../models/LtiResourceLink"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_lti_resource_link = async (token: string, baseUrl: string, params: {
  "course_id": string;"url": string;"title"?: string;"custom"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as LtiResourceLink
}
