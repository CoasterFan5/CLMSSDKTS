 //Generated by CoasterFan5
  const baseUrl = "/sis/courses/{course_id}/disable_post_to_sis";
  

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const disable_assignments_currently_enabled_for_grade_export_to_sis = async (token: string, baseUrl: string, params: {
  "course_id": number;"grading_period_id"?: number
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as void
}
