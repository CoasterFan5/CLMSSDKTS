 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/assignment_groups/{assignment_group_id}";
  import type {AssignmentGroup} from "../models/AssignmentGroup"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const edit_assignment_group = async (token: string, baseUrl: string, params: {
  "course_id": string;"assignment_group_id": string;"name"?: string;"position"?: number;"group_weight"?: number;"sis_source_id"?: string;"integration_data"?: undefined;"rules"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as AssignmentGroup
}
