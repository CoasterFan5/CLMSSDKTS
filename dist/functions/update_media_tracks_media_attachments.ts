 //Generated by CoasterFan5
  const baseUrl = "/v1/media_attachments/{attachment_id}/media_tracks";
  import type {MediaTrack} from "../models/MediaTrack"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_media_tracks_media_attachments = async (token: string, baseUrl: string, params: {
  "attachment_id": string;"include"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as MediaTrack[]
}
