 //Generated by CoasterFan5
  const baseUrl = "/v1/groups/{group_id}/users/{user_id}";
  import type {GroupMembership} from "../models/GroupMembership"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_single_group_membership_users = async (token: string, baseUrl: string, params: {
  "group_id": string;"user_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as GroupMembership
}
