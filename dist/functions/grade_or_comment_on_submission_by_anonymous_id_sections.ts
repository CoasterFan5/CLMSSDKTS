 //Generated by CoasterFan5
  const baseUrl = "/v1/sections/{section_id}/assignments/{assignment_id}/anonymous_submissions/{anonymous_id}";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const grade_or_comment_on_submission_by_anonymous_id_sections = async (token: string, baseUrl: string, params: {
  "section_id": string;"assignment_id": string;"anonymous_id": string;"comment[text_comment]"?: string;"comment[group_comment]"?: boolean;"comment[media_comment_id]"?: string;"comment[media_comment_type]"?: string;"comment[file_ids]"?: unknown;"include[visibility]"?: string;"submission[posted_grade]"?: string;"submission[excuse]"?: boolean;"submission[late_policy_status]"?: string;"submission[seconds_late_override]"?: number;"rubric_assessment"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
