 //Generated by CoasterFan5
  const baseUrl = "/v1/users/{user_id}/enrollments";
  import type {Enrollment} from "../models/Enrollment"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_enrollments_users = async (token: string, baseUrl: string, params: {
  "type"?: unknown;"role"?: unknown;"state"?: unknown;"include"?: unknown;"user_id": string;"grading_period_id"?: number;"enrollment_term_id"?: number;"sis_account_id"?: unknown;"sis_course_id"?: unknown;"sis_section_id"?: unknown;"sis_user_id"?: unknown;"created_for_sis_id"?: unknown
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as Enrollment[]
}
