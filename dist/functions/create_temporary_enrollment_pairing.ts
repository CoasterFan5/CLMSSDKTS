 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/temporary_enrollment_pairings";
  import type {TemporaryEnrollmentPairing} from "../models/TemporaryEnrollmentPairing"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_temporary_enrollment_pairing = async (token: string, baseUrl: string, params: {
  "account_id": string;"workflow_state"?: string;"ending_enrollment_state"?: string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as TemporaryEnrollmentPairing
}
