 //Generated by CoasterFan5
  const baseUrl = "/v1/account_calendars/{account_id}";
  import type {AccountCalendar} from "../models/AccountCalendar"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_calendar = async (token: string, baseUrl: string, params: {
  "account_id": string;"visible"?: boolean;"auto_subscribe"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as AccountCalendar
}
