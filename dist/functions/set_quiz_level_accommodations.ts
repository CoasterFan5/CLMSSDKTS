 //Generated by CoasterFan5
  const baseUrl = "/quiz/v1/courses/{course_id}/quizzes/{assignment_id}/accommodations";
  import type {AccommodationResponse} from "../models/AccommodationResponse"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const set_quiz_level_accommodations = async (token: string, baseUrl: string, params: {
  "course_id": string;"assignment_id": number;"user_id": number;"extra_time"?: number;"extra_attempts"?: number;"reduce_choices_enabled"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as AccommodationResponse
}
