 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/block_editor_templates";
  import type {BlockEditorTemplate} from "../models/BlockEditorTemplate"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_block_templates = async (token: string, baseUrl: string, params: {
  "course_id": string;"sort"?: string;"order"?: string;"drafts"?: boolean;"type"?: unknown;"include"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as BlockEditorTemplate[]
}
