 //Generated by CoasterFan5
  const baseUrl = "/lti/assignments/{assignment_id}/files/{file_id}/originality_report";
  import type {OriginalityReport} from "../models/OriginalityReport"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const edit_originality_report_files = async (token: string, baseUrl: string, params: {
  "assignment_id": string;"file_id": string;"originality_report[originality_score]"?: number;"originality_report[originality_report_url]"?: string;"originality_report[originality_report_file_id]"?: number;"originality_report[tool_setting][resource_type_code]"?: string;"originality_report[tool_setting][resource_url]"?: string;"originality_report[workflow_state]"?: string;"originality_report[error_message]"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as OriginalityReport
}
