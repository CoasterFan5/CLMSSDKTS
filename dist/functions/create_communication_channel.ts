 //Generated by CoasterFan5
  const baseUrl = "/v1/users/{user_id}/communication_channels";
  import type {CommunicationChannel} from "../models/CommunicationChannel"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_communication_channel = async (token: string, baseUrl: string, params: {
  "user_id": string;"communication_channel[address]": string;"communication_channel[type]": string;"communication_channel[token]"?: string;"skip_confirmation"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as CommunicationChannel
}
