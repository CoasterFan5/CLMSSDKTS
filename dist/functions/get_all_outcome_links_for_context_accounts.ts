 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/outcome_group_links";
  import type {OutcomeLink} from "../models/OutcomeLink"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_all_outcome_links_for_context_accounts = async (token: string, baseUrl: string, params: {
  "account_id": string;"outcome_style"?: string;"outcome_group_style"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as OutcomeLink[]
}
