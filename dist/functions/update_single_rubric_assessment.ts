 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/rubric_associations/{rubric_association_id}/rubric_assessments/{id}";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_single_rubric_assessment = async (token: string, baseUrl: string, params: {
  "id": number;"course_id": number;"rubric_association_id": number;"provisional"?: string;"final"?: string;"graded_anonymously"?: boolean;"rubric_assessment"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
