 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{id}/late_policy";
  

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_late_policy = async (token: string, baseUrl: string, params: {
  "id": string;"late_policy[missing_submission_deduction_enabled]"?: boolean;"late_policy[missing_submission_deduction]"?: number;"late_policy[late_submission_deduction_enabled]"?: boolean;"late_policy[late_submission_deduction]"?: number;"late_policy[late_submission_interval]"?: string;"late_policy[late_submission_minimum_percent_enabled]"?: boolean;"late_policy[late_submission_minimum_percent]"?: number
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
