 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/lti_registration_by_client_id/{client_id}";
  import type {LtiRegistration} from "../models/LtiRegistration"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_lti_registration_via_client_id = async (token: string, baseUrl: string, params: {
  "account_id": string;"client_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as LtiRegistration
}
