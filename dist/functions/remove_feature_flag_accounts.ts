 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/features/flags/{feature}";
  import type {FeatureFlag} from "../models/FeatureFlag"

  const method = "DELETE"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const remove_feature_flag_accounts = async (token: string, baseUrl: string, params: {
  "account_id": string;"feature": string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as FeatureFlag
}
