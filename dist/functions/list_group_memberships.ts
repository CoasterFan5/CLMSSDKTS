 //Generated by CoasterFan5
  const baseUrl = "/lti/groups/{group_id}/names_and_roles";
  import type {NamesAndRoleMemberships} from "../models/NamesAndRoleMemberships"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_group_memberships = async (token: string, baseUrl: string, params: {
  "group_id": string;"`rlid`"?: string;"role"?: string;"limit"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as NamesAndRoleMemberships
}
