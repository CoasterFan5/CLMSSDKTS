 //Generated by CoasterFan5
  const baseUrl = "/lti/courses/{course_id}/line_items";
  import type {LineItem} from "../models/LineItem"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_line_item = async (token: string, baseUrl: string, params: {
  "course_id": string;"scoreMaximum": number;"label": string;"resourceId"?: string;"tag"?: string;"resourceLinkId"?: string;"startDateTime"?: string;"endDateTime"?: string;"https://canvas.instructure.com/lti/submission_type"?: object
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as LineItem
}
