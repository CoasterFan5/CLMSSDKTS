 //Generated by CoasterFan5
  const baseUrl = "/lti/courses/{course_id}/line_items";
  import type {LineItem} from "../models/LineItem"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_line_items = async (token: string, baseUrl: string, params: {
  "course_id": string;"tag"?: string;"resource_id"?: string;"resource_link_id"?: string;"limit"?: string;"include"?: unknown
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as LineItem
}
