 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/outcome_groups/{id}/import";
  import type {OutcomeGroup} from "../models/OutcomeGroup"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const import_outcome_group_courses = async (token: string, baseUrl: string, params: {
  "course_id": string;"id": string;"source_outcome_group_id": number;"async"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as OutcomeGroup
}
