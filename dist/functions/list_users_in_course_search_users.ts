 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/search_users";
  import type {User} from "../models/User"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_users_in_course_search_users = async (token: string, baseUrl: string, params: {
  "course_id": string;"search_term"?: string;"sort"?: string;"enrollment_type"?: unknown;"enrollment_role"?: string;"enrollment_role_id"?: number;"include"?: unknown;"user_id"?: string;"user_ids"?: unknown;"enrollment_state"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as User[]
}
