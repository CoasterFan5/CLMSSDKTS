 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/sub_accounts";
  import type {Account} from "../models/Account"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_sub_accounts_of_account = async (token: string, baseUrl: string, params: {
  "account_id": string;"recursive"?: boolean;"order"?: string;"include"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Account[]
}
