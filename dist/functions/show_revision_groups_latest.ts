 //Generated by CoasterFan5
  const baseUrl = "/v1/groups/{group_id}/pages/{url_or_id}/revisions/latest";
  import type {PageRevision} from "../models/PageRevision"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_revision_groups_latest = async (token: string, baseUrl: string, params: {
  "group_id": string;"url_or_id": string;"summary"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as PageRevision
}
