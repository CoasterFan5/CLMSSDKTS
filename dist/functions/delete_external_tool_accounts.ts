 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/external_tools/{external_tool_id}";
  

  const method = "DELETE"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const delete_external_tool_accounts = async (token: string, baseUrl: string, params: {
  "account_id": string;"external_tool_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
