 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/files/{attachment_id}/date_details";
  import type {LearningObjectDates} from "../models/LearningObjectDates"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_learning_object_s_date_information_files = async (token: string, baseUrl: string, params: {
  "course_id": string;"attachment_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as LearningObjectDates
}
