 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/assignment_groups/{assignment_group_id}";
  import type {AssignmentGroup} from "../models/AssignmentGroup"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_assignment_group = async (token: string, baseUrl: string, params: {
  "course_id": string;"assignment_group_id": string;"include"?: unknown;"override_assignment_dates"?: boolean;"grading_period_id"?: number
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as AssignmentGroup
}
