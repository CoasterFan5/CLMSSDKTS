 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/calendar_events/timetable";
  

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const set_course_timetable = async (token: string, baseUrl: string, params: {
  "course_id": string;"timetables[course_section_id]"?: unknown;"timetables[course_section_id][weekdays]"?: unknown;"timetables[course_section_id][start_time]"?: unknown;"timetables[course_section_id][end_time]"?: unknown;"timetables[course_section_id][location_name]"?: unknown
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as void
}
