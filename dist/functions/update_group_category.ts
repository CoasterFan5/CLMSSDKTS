 //Generated by CoasterFan5
  const baseUrl = "/v1/group_categories/{group_category_id}";
  import type {GroupCategory} from "../models/GroupCategory"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_group_category = async (token: string, baseUrl: string, params: {
  "group_category_id": string;"name"?: string;"self_signup"?: string;"auto_leader"?: string;"group_limit"?: number;"sis_group_category_id"?: string;"create_group_count"?: number;"split_group_count"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as GroupCategory
}
