 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/discussion_topics/{topic_id}/entries/{entry_id}/read";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const mark_entry_as_read_courses = async (token: string, baseUrl: string, params: {
  "course_id": string;"topic_id": string;"entry_id": string;"forced_read_state"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
