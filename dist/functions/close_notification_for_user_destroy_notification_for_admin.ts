 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/account_notifications/{id}";
  import type {AccountNotification} from "../models/AccountNotification"

  const method = "DELETE"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const close_notification_for_user_destroy_notification_for_admin = async (token: string, baseUrl: string, params: {
  "account_id": string;"id": string;"remove"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as AccountNotification
}
