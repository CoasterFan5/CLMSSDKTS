 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/outcome_results";
  

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_outcome_results = async (token: string, baseUrl: string, params: {
  "course_id": string;"user_ids"?: unknown;"outcome_ids"?: unknown;"include"?: unknown;"include_hidden"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
