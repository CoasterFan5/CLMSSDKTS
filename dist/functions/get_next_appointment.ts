 //Generated by CoasterFan5
  const baseUrl = "/v1/appointment_groups/next_appointment";
  import type {CalendarEvent} from "../models/CalendarEvent"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_next_appointment = async (token: string, baseUrl: string, params: {
  "appointment_group_ids"?: unknown
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as CalendarEvent[]
}
