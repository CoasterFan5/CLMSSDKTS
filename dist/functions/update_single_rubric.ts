 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/rubrics/{id}";
  

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_single_rubric = async (token: string, baseUrl: string, params: {
  "course_id": string;"id": number;"rubric_association_id"?: number;"rubric[title]"?: string;"rubric[free_form_criterion_comments]"?: boolean;"rubric[skip_updating_points_possible]"?: boolean;"rubric_association[association_id]"?: number;"rubric_association[association_type]"?: string;"rubric_association[use_for_grading]"?: boolean;"rubric_association[hide_score_total]"?: boolean;"rubric_association[purpose]"?: string;"rubric[criteria]"?: undefined
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as void
}
