 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/files/{id}";
  import type {File} from "../models/File"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const get_file_courses = async (token: string, baseUrl: string, params: {
  "course_id": string;"id": string;"include"?: unknown;"replacement_chain_context_type"?: string;"replacement_chain_context_id"?: number
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as File
}
