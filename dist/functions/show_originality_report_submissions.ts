 //Generated by CoasterFan5
  const baseUrl = "/lti/assignments/{assignment_id}/submissions/{submission_id}/originality_report/{id}";
  import type {OriginalityReport} from "../models/OriginalityReport"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_originality_report_submissions = async (token: string, baseUrl: string, params: {
  "assignment_id": string;"submission_id": string;"id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as OriginalityReport
}
