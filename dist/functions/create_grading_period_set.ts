 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/grading_period_sets";
  

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_grading_period_set = async (token: string, baseUrl: string, params: {
  "account_id": string;"enrollment_term_ids"?: unknown;"grading_period_set[title]": string;"grading_period_set[weighted]"?: boolean;"grading_period_set[display_totals_for_all_grading_periods]"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
