 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/quizzes/{quiz_id}/groups/{id}";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_question_group = async (token: string, baseUrl: string, params: {
  "course_id": string;"quiz_id": string;"id": string;"quiz_groups[name]"?: unknown;"quiz_groups[pick_count]"?: unknown;"quiz_groups[question_points]"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
