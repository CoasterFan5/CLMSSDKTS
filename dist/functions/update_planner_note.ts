 //Generated by CoasterFan5
  const baseUrl = "/v1/planner_notes/{id}";
  import type {PlannerNote} from "../models/PlannerNote"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_planner_note = async (token: string, baseUrl: string, params: {
  "id": string;"title"?: string;"details"?: string;"todo_date"?: undefined;"course_id"?: number
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as PlannerNote
}
