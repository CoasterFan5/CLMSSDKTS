 //Generated by CoasterFan5
  const baseUrl = "/v1/groups/{group_id}/discussion_topics";
  import type {DiscussionTopic} from "../models/DiscussionTopic"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_discussion_topics_groups = async (token: string, baseUrl: string, params: {
  "group_id": string;"include"?: unknown;"order_by"?: string;"scope"?: string;"only_announcements"?: boolean;"filter_by"?: string;"search_term"?: string;"exclude_context_module_locked_topics"?: boolean
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as DiscussionTopic[]
}
