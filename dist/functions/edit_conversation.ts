 //Generated by CoasterFan5
  const baseUrl = "/v1/conversations/{id}";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const edit_conversation = async (token: string, baseUrl: string, params: {
  "id": string;"conversation[workflow_state]"?: string;"conversation[subscribed]"?: boolean;"conversation[starred]"?: boolean;"scope"?: string;"filter"?: unknown;"filter_mode"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
