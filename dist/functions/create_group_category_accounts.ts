 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/group_categories";
  import type {GroupCategory} from "../models/GroupCategory"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_group_category_accounts = async (token: string, baseUrl: string, params: {
  "account_id": string;"name": string;"non_collaborative"?: boolean;"self_signup"?: string;"auto_leader"?: string;"group_limit"?: number;"sis_group_category_id"?: string;"create_group_count"?: number;"split_group_count"?: string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as GroupCategory
}
