 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/logins";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_user_login = async (token: string, baseUrl: string, params: {
  "account_id": string;"user[id]": string;"login[unique_id]": string;"login[password]"?: string;"login[sis_user_id]"?: string;"login[integration_id]"?: string;"login[authentication_provider_id]"?: string;"login[declared_user_type]"?: string;"user[existing_user_id]"?: string;"user[existing_integration_id]"?: string;"user[existing_sis_user_id]"?: string;"user[trusted_account]"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
