 //Generated by CoasterFan5
  const baseUrl = "/v1/polls/{poll_id}/poll_sessions/{poll_session_id}/poll_submissions";
  

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_single_poll_submission = async (token: string, baseUrl: string, params: {
  "poll_id": string;"poll_session_id": string;"poll_submissions[poll_choice_id]": unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
