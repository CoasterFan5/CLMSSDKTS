 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/grading_periods/{id}";
  


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_single_grading_period = async (token: string, baseUrl: string, params: {
  "course_id": string;"id": string;"grading_periods[start_date]": unknown;"grading_periods[end_date]": unknown;"grading_periods[weight]"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as void
}
