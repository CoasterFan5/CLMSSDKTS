 //Generated by CoasterFan5
  const baseUrl = "/v1/folders/{folder_id}/folders";
  import type {Folder} from "../models/Folder"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_folder_folders = async (token: string, baseUrl: string, params: {
  "folder_id": string;"name": string;"parent_folder_id"?: string;"parent_folder_path"?: string;"lock_at"?: undefined;"unlock_at"?: undefined;"locked"?: boolean;"hidden"?: boolean;"position"?: number
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Folder
}
