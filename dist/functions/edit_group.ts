 //Generated by CoasterFan5
  const baseUrl = "/v1/groups/{group_id}";
  import type {Group} from "../models/Group"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const edit_group = async (token: string, baseUrl: string, params: {
  "group_id": string;"name"?: string;"description"?: string;"is_public"?: boolean;"join_level"?: string;"avatar_id"?: number;"storage_quota_mb"?: number;"members"?: unknown;"sis_group_id"?: string;"override_sis_stickiness"?: boolean
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as Group
}
