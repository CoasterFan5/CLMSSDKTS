 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/media_attachments";
  import type {MediaObject} from "../models/MediaObject"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const list_media_objects_courses_media_attachments = async (token: string, baseUrl: string, params: {
  "course_id": string;"sort"?: string;"order"?: string;"exclude"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as MediaObject[]
}
