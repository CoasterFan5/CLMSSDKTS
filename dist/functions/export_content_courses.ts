 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/content_exports";
  import type {ContentExport} from "../models/ContentExport"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const export_content_courses = async (token: string, baseUrl: string, params: {
  "course_id": string;"export_type": string;"skip_notifications"?: boolean;"select"?: undefined
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as ContentExport
}
