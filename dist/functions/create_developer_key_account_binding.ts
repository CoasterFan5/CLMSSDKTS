 //Generated by CoasterFan5
  const baseUrl = "/v1/accounts/{account_id}/developer_keys/{developer_key_id}/developer_key_account_bindings";
  import type {DeveloperKeyAccountBinding} from "../models/DeveloperKeyAccountBinding"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const create_developer_key_account_binding = async (token: string, baseUrl: string, params: {
  "account_id": string;"developer_key_id": string;"workflow_state"?: string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as DeveloperKeyAccountBinding
}
