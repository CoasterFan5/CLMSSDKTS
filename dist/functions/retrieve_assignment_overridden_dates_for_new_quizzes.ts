 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/new_quizzes/assignment_overrides";
  import type {QuizAssignmentOverrideSetContainer} from "../models/QuizAssignmentOverrideSetContainer"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const retrieve_assignment_overridden_dates_for_new_quizzes = async (token: string, baseUrl: string, params: {
  "course_id": string;"quiz_assignment_overrides[quiz_ids]"?: unknown
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as QuizAssignmentOverrideSetContainer
}
