 //Generated by CoasterFan5
  const baseUrl = "/v1/sections/{id}/crosslist";
  import type {Section} from "../models/Section"


  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const de_cross_list_section = async (token: string, baseUrl: string, params: {
  "id": string;"override_sis_stickiness"?: boolean
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Section
}
