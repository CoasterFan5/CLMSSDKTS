 //Generated by CoasterFan5
  const baseUrl = "/v1/groups/{group_id}/memberships/{membership_id}";
  import type {GroupMembership} from "../models/GroupMembership"

  const method = "PUT"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const update_membership_memberships = async (token: string, baseUrl: string, params: {
  "group_id": string;"membership_id": string;"workflow_state"?: string;"moderator"?: string
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as GroupMembership
}
