 //Generated by CoasterFan5
  const baseUrl = "/v1/courses/{course_id}/pages/{url_or_id}/revisions/latest";
  import type {PageRevision} from "../models/PageRevision"

  const method = "GET"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const show_revision_courses_latest = async (token: string, baseUrl: string, params: {
  "course_id": string;"url_or_id": string;"summary"?: boolean
}) => {
  const r = await fetch(urlBuilder(baseUrl, params) + "?per_page=100", {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    throw r
    return undefined
  }
  return (await r.json()) as PageRevision
}
