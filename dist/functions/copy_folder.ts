 //Generated by CoasterFan5
  const baseUrl = "/v1/folders/{dest_folder_id}/copy_folder";
  import type {Folder} from "../models/Folder"

  const method = "POST"

  const urlBuilder = (base: string, items: Record<string, unknown>) => {
    let newUrl = baseUrl
    for (const key in items) {
      newUrl = newUrl.replaceAll(`{${key}}`, `${items[key]}`)
    }

    return base + newUrl
  }

export const copy_folder = async (token: string, baseUrl: string, params: {
  "dest_folder_id": string;"source_folder_id": string
}) => {
  console.log(token)
  const r = await fetch(urlBuilder(baseUrl, params), {
    method: method,
    headers: {
      "Authorization": "Bearer " + token
    }
  })
  if(r.status != 200) {
    console.error(r)
    return undefined
  }
  return (await r.json()) as Folder
}
